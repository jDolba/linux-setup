#!/usr/bin/env bash
# Run an explicit SSH operation using keyKeeper's isolated SSH credentials.
set -Eeuo pipefail

if (($# == 0)); then
    printf 'Usage: keykeeper-ssh <ssh arguments>\n' >&2
    exit 2
fi

exec sudo -u keyKeeper -- ssh "$@"
